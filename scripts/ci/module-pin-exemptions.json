{
  "$comment": [
    "Records in `modules::registry::ALL` whose submodule pin deliberately does",
    "NOT sit on the tag their `version` names. See scripts/ci/check-module-pins.mjs.",
    "",
    "An entry here is a claim that the drift is known and accepted, NOT a way to",
    "silence the gate: `expect` pins the exact `git describe --tags` output, so a",
    "record already exempt cannot drift FURTHER without failing. Widening the drift",
    "is a deliberate edit to this file, which is a reviewable diff.",
    "",
    "Delete an entry the moment the pins are reconciled. An exemption that has",
    "stopped being true fails the gate too — `expect` must still match."
  ],
  "exemptions": [
    {
      "id": "tinymcp",
      "submodule": "vendor/tinymcp",
      "expect": "v0.3.2",
      "reason": "The v0.3.2 source tag is the current compile-time contract, but no v0.3.2 GitHub Release artifact exists. The registry therefore deliberately retains the last published v0.3.1 runtime artifact."
    },
    {
      "id": "tinywallet",
      "submodule": "vendor/tinywallet",
      "expect": "v0.5.1",
      "reason": "The v0.5.1 source tag is the current compile-time contract, but no v0.5.1 GitHub Release artifact exists. The registry deliberately retains the last published v0.5.0 runtime artifact."
    },
    {
      "id": "tinydocs",
      "submodule": "vendor/tinydocs",
      "expect": "v0.1.15",
      "reason": "The v0.1.15 source tag is current, but no corresponding GitHub Release artifact exists; the registry retains the last published runtime artifact."
    },
    {
      "id": "tinymemory",
      "submodule": "vendor/tinymemory",
      "expect": "v1.13.4-31-g97d3e08",
      "reason": "The current source revision is ahead of v1.13.4 and has no published GitHub Release artifact; registry and CI retain the last published runtime artifact set."
    },
    {
      "id": "tinyjuice",
      "submodule": "vendor/tinyjuice",
      "expect": "v0.2.5",
      "reason": "The v0.2.5 source tag is current, but no corresponding GitHub Release artifact exists; the registry retains the last published runtime artifact."
    },
    {
      "id": "tinyvoice",
      "submodule": "vendor/tinyvoice",
      "expect": "v0.1.6",
      "reason": "The v0.1.6 source tag is current, but no corresponding GitHub Release artifact exists; the registry retains the last published runtime artifact."
    },
    {
      "id": "tinyruntime",
      "submodule": "vendor/tinyruntime",
      "expect": "v0.2.4",
      "reason": "The v0.2.4 source tag is current, but no corresponding GitHub Release artifact exists; the registry retains the last published runtime artifact."
    },
    {
      "id": "tinyruntime-nodejs",
      "submodule": "vendor/tinyruntime",
      "expect": "v0.2.4",
      "reason": "Published from tinyruntime; the current source tag has no corresponding GitHub Release artifact, so the registry retains the last published provider artifact."
    },
    {
      "id": "tinyruntime-python",
      "submodule": "vendor/tinyruntime",
      "expect": "v0.2.4",
      "reason": "Published from tinyruntime; the current source tag has no corresponding GitHub Release artifact, so the registry retains the last published provider artifact."
    },
    {
      "id": "tinyconnectors",
      "submodule": "vendor/tinyconnectors",
      "expect": "v0.7.1",
      "reason": "The v0.7.1 source tag is current, but no corresponding GitHub Release artifact exists; the registry retains the last published runtime artifact."
    }
  ]
}
